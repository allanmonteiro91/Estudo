/* Desafio
- Crie 5 variáveis, cada uma de um tipo primitivo visto acima
- Use console.log() para imprimir o valor e o tipo de cada variável
*/

let nome = 'João';
const idade = 25;
const estudante = true;
let cidade;
let vazio = null;

console.log(`${nome} (${typeof nome})`);
console.log(`${idade} (${typeof idade})`);
console.log(`${estudante} (${typeof estudante})`);
console.log(`${cidade} (${typeof cidade})`);
console.log(`${vazio} (${typeof vazio})`); // vai retornar 'object'
